# 首版验收记录

日期：2026-09-18。默认文字模式，无外部模型或第三方媒体。

## 已验证

- 独立目录 `npm ci` 可安装；没有父目录符号链接或原站点配置，依赖版本和锁文件保持不变。
- planner、product-state、ai-client、mvp-lifecycle、open-source 五组定向测试，共 54 项通过。
- 10 个 JSX 文件单文件语法检查通过，暂存差异空白检查通过。
- 内置浏览器：无密钥生成计划；记录第 1 组实际次数 9 并完成；出现 45 秒休息计时；刷新后组次与次数保留。
- 浏览器模拟完成 4 个动作共 8 组；结束页显示完成 8、跳过 0、未完成 0；保存后历史记录显示 8/8，首页轮转到训练 B；保留原计划可关闭建议。此处是软件操作测试，不是实际身体训练或效果验证。
- 桌面 1440×1000 与手机 390×844 检查。手机筛选为两列，计划按钮单独一行，不再挤成竖排。
- 仓库不含第三方动作图片/GIF，不含个人托管配置；资源路径只允许受限的同站点地址，默认禁用媒体。

## 未验证与限制

- 真实模型、浏览器跨域兼容、费用与服务稳定性。
- 浏览器文件选择导入和跨浏览器恢复（序列化和损坏输入已有定向测试）。
- 真机、弱网、长期 localStorage 容量、完整键盘/读屏与对比度评估。
- 用户自备媒体的使用授权，以及启用媒体后的各类文件组合。
- 训练内容的专业适用性。产品标签和软件测试不能替代专业审核。

本地只执行相关测试与浏览器检查；完整测试及生产构建由 GitHub Actions 执行，以对应提交的实际 CI 状态为准。本记录不代表发布了演示站点，也不代表上述未验证项已通过。
