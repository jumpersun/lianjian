# All VITE_ values are PUBLIC. Never put API keys in this file.
# Optional compatible server endpoint; empty means local rule-based planning.
VITE_AI_PLAN_ENDPOINT=
# Enable only after supplying your own lawfully licensed local images/videos.
VITE_ENABLE_EXERCISE_MEDIA=false
