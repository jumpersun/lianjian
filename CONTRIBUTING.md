# 贡献指南 · Contributing

欢迎提交聚焦的小改动。较大的功能、数据结构变更或新增服务，请先开 Issue 讨论。

1. Fork 仓库并创建功能分支。
2. 使用 Node.js 24，执行 `npm ci`、`npm run dev`。
3. 为规则、状态、模型校验或备份逻辑增加相关测试；UI 修改附上脱敏的桌面/手机截图和复现步骤。
4. 提交 PR 前执行 `npm test` 和 `npm run build`，说明行为变化及未验证边界。

## 约定

- 保持 local-first，无密钥路径必须可用；不要以假登录或伪造 AI 响应代表真实能力。
- 不扩大模型可选择的动作范围来掩盖数据不足；未核验动作不进入自动推荐池。
- 不让计划编辑或异步模型结果覆盖已保存训练历史。
- 动作名称/教学更正请附可靠来源与对应 ID，注明是翻译、标签还是源数据问题；不要声称专业认证。
- 不提交密钥、真实健康信息、个人部署配置或第三方图片/GIF。数据导入和清洗也须保留来源及许可。
- 使用现有组件和依赖，避免无关格式化或顺手升级依赖。
- Chinese and English issues/PRs are welcome. Keep changes focused and include reproducible steps.

## 安全问题

不要在公开 Issue 中贴出密钥或私密数据。请按 [SECURITY.md](SECURITY.md) 的私密报告流程提交。

提交贡献表示你有权提供这些改动，并同意按本项目相应许可证分发；第三方材料必须保留其原许可，不得假定由项目 MIT 许可证覆盖。
